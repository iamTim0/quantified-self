[project]
name = "qs-core-migration-runtime"
version = "0.1.0"
description = "Minimal runtime for applying Core database migrations"
license = "AGPL-3.0-only"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.13.0",
    "asyncpg>=0.29.0",
    "geoalchemy2>=0.14.0",
    "pgvector>=0.2.5",
    "pydantic-settings>=2.2.0",
    "sqlalchemy[asyncio]>=2.0.0",
]

[tool.uv]
package = false
