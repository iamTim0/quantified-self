# Home Assistant Importer

Stateless, request-driven importer. It retrieves encrypted connector credentials dynamically from Core and publishes tenant-scoped, idempotent events to `qs.ingest.home_assistant`.
