[project]
name="weather-importer"
version="0.1.0"
license = "MIT"
requires-python=">=3.12"
dependencies=["httpx>=0.27","nats-py>=2.8","pydantic-settings>=2.2","pytest>=8","pytest-asyncio>=0.24"]
[build-system]
requires=["hatchling"]
build-backend="hatchling.build"
[tool.hatch.build.targets.wheel]
packages=["src/weather_importer"]
