"use client";

import OverviewPage from "../page";

/** `/dashboard` is an alias for the overview at `/`, kept for existing links. */
export default function DashboardRoutePage() {
  return <OverviewPage />;
}
